
<nav class="navbar navbar-expand navbar-dark bg-primary">
  <div class="container-fluid">
    <a class="navbar-brand" href="/">Lille-Events</a>

    <ul class="navbar-nav mr-auto">
      <% if user_signed_in? %>
        <li class="nav-item">
          <%= link_to current_user.first_name, user_path(current_user), class:"nav-link" %>
        </li>
        <li class="nav-item">
          <%= link_to "Déconnexion", destroy_user_session_path, method: :delete, class:"nav-link" %>
        </li>
        <% if current_user.is_global_admin? %>
          <li class="nav-item"><%= link_to "Espace admin", "/admin", class:"nav-link" %></li>
        <% end %>
      </ul>
      <div class="navbar-nav">
        <%= link_to "Créer un évènement", new_event_path, class:"btn btn-secondary" %>
      </div>
      <% else %>
        <li class="nav-item">
          <%= link_to "Connexion", new_user_session_path, class:"nav-link" %>
        </li>
        <li class="nav-item">
          <%= link_to "Inscription", new_user_registration_path, class:"nav-link" %>
        </li>
      </ul>
      <% end %>

  </div>
</nav>
